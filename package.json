{
  "name": "md-feedback-monorepo",
  "private": true,
  "version": "0.9.6",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "package": "pnpm --filter md-feedback-vscode package",
    "clean": "pnpm -r exec rm -rf dist",
    "guard:public-surface": "node scripts/guard-public-surface.mjs",
    "sync-version": "node scripts/sync-version.mjs",
    "verify-release": "node scripts/verify-release.mjs",
    "release": "node scripts/release.mjs",
    "publish:npm": "cd apps/mcp-server && npm publish",
    "publish:vsce": "cd apps/vscode && vsce publish",
    "publish:ovsx": "cd apps/vscode && ovsx publish"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
