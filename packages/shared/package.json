{
  "name": "@md-feedback/shared",
  "version": "1.5.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'shared: source-only package, no build needed'",
    "test": "vitest run",
    "test:name": "vitest run -t"
  }
}
