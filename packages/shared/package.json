{
  "name": "@md-feedback/shared",
  "version": "0.9.5",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'shared: source-only package, no build needed'",
    "test": "vitest run"
  }
}
