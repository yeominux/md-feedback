{
  "name": "@md-feedback/shared",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'shared: source-only package, no build needed'",
    "test": "vitest run"
  }
}
