{
  "name": "@md-feedback/shared",
  "version": "1.3.5",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'shared: source-only package, no build needed'",
    "test": "vitest run"
  }
}
